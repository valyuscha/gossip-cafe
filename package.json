{
  "name": "gossip-cafe",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm start",
    "build": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build",
    "start": "npx serve -s frontend/build -l ${PORT:-3000}",
    "install:frontend": "cd frontend && npm install --legacy-peer-deps"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": "18.x"
  }
}
